import React from "react";
import PublishPoolFormCard from "../components/publish_Pool/PublishPoolFormCard";

const UserPublishPool = () => {
  return (
    <div className="col-md-9 userProfile-main">
      <div className="container">
        <PublishPoolFormCard />
      </div>
    </div>
  );
};

export default UserPublishPool;
